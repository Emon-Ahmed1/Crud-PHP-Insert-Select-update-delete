<?php /*
define('SERVER', 'localhost');
define('USERNAME', 'root');
define('PASSWORD', '');
define('DATABASE', 'curdapp');

class curdApp {
    function __construct(){
        $connection = mysqli_connect(SERVER, USERNAME, PASSWORD, DATABASE);
        if(!$connection){
            die('database connect error');
        }
    }
} */
?> 
 